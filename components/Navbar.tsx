import React from 'react'

const Navbar = () => {
  return (
    <header>
      <nav>
        <span>Logo</span>
        <span>Menu</span>
        <span>CTA</span>
      </nav>
    </header>
  )
}

export default Navbar